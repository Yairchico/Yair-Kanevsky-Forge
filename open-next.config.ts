// See https://opennext.js.org/cloudflare for available overrides
// (e.g. an R2-backed incremental cache once ISR/data caching is needed).
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig();
